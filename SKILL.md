---
name: douni-customer-submission
description: 手动选择每日提报日期范围，将已内审达人向对应客户页提报。先生成单卡预览，原发起人确认后执行；不用于选题生成、客户结果回填或发布补登。
---

# 客户提报

使用 CustomerSubmissionService 与 CustomerSubmissionLarkPorts。内部会话说“向客户提报”或“补提报”后，先发一张 Card 2.0 范围卡：发起人选择每日提报表的开始、结束日期，可再限定唯一 ID。19:00 只是团队协作时间，不设自动写客户表的定时任务。

## 范围与来源

1. 只扫描卡片选择日期内的每日提报表，不扫描其他历史日表；ID 留空按所选日期全部处理。
2. 每日提报表是尚未外提合作的内审权威源。机构＋唯一 ID 定位机构记录；达人名称用忽略标点、空格及 emoji 的规范化结果辅助核对。主页链接不参与机构记录定位。
3. 同一机构、同一 ID 在同一天出现多条时暂停；正常情况下查重阶段已拦截，不额外做跨历史版本拼接。
4. 普通提报不做客户页历史交叉恢复。提报客户时间非空、本地台账存在或已有客户终态时直接保护。真正写入前仍做一次精确防重回读，发现客户页已有完全匹配行只暂停，不自动建立历史关联，也不重复插行。

## 机构回填与客户门槛

1. 向客户提报前只回填“内审不通过”的提报审核及选题/备注；选题/备注保留否决原因原文。内审通过不回填机构，查重结果、档期、进度、附件、价格等均不回填。
2. 只有内审通过、路由明确、客户目标页实际需要的字段齐全、提报客户时间为空的记录才可新增。机构和唯一 ID 是内部定位安全字段，不写进客户备注或新增客户 ID 列。
3. 实际执行日写入客户表“提报时间”。客户页成功回读后，才把同一日期登记到机构表和原每日提报表。
4. 预览确认后按预览快照执行，不因普通选题或档期编辑自动换成另一版；团队需保证预览后不再改本批。若出现已提报日期、客户终态、本地台账或同日重复，仍以防重优先并暂停。

## 路由与字段

路由优先级：

1. 进度备注为明确的“赚钱”标记 → 赚钱赛道提报页。
2. 明确属于独立端或双端 → 独立端提报页。
3. 小红书且未命中前两项 → 小红书提报页。
4. 抖音 PC 工作任务或课程教学 → 抖音工作任务提报页。

无法明确分类则待核。选题正文偶然出现“赚钱”不改变路由。独立端客户页的一行可以同时承载该达人独立端和 PC 端两组选题、档期；有明确分端内容就分别填写，资料缺失不复制同一选题凑两期。

客户字段按共享配置映射。客户拥有的审核、反馈、方向、客户备注等保持空白；内部否决原因和进度备注不对外复制。四张表均在表头下方插入，保留旧数据、样式、行高和下拉。

### 小红书报价

报价只由合作形式与粉丝量（W）确定，不再读取机构公式价：

| 粉丝量 | 视频 | 图文 |
|---|---:|---:|
| 1–3W | 4000 | 1500 |
| 大于 3–5W | 7000 | 3000 |
| 大于 5–10W | 10300 | 4500 |
| 大于 10W | 10300 | 4500 |

必须明确区分“视频”与“图文”。同时包含两种、无法识别形式或粉丝量低于 1W时暂停，不猜报价。

## 预览、确认与交付

1. 范围卡提交后才扫描。完整拟提报字段、内审不通过回填及待核原因写进一份明细表。
2. 群内只发一张紧凑预览卡，不发送逐条长文本、不分段刷屏。预览卡只包含总数、目标页汇总、待核数和明细表按钮。
3. 原发起人直接回复预览卡“确认”或“执行”，12 小时内有效。确认后只发一张明确标注“已处理完”的结果卡，必须列出客户表新增、内审不通过回填、回执恢复和待核数量。
4. 明细表或结果卡创建失败时只发一条短回执；若业务写入已经产生结果，短回执仍须报告真实成功数和待核数，不允许只报笼统错误，也不降级刷屏。

## 可靠性

- 客户写请求超时先回读；无法确认时冻结该目标页新增，其他目标页继续。
- 已提交内容不由“补提报”修改；修订已提报须走单独明确确认。
- 提交台账、计划、锁及通知回执是长期业务数据，不随缓存和日志清理。
- Skill 不自行新增定时任务，也不扩大到发布、产能、水评或爆款数据源。
